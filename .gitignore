*.iml
.gradle/
/local.properties
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
.idea/
*.apk
*.aab
*.keystore
*.jks
# debug keystore is the well-known Android default and is safe to commit
# so every build machine signs debug APKs identically; the release keystore
# is private and must be supplied at build time via env vars.
!app/debug.keystore
